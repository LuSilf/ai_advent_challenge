OPENAI_API_KEY_ENV=OPENAI_API_KEY
OPENAI_MODEL=gpt-4o-mini
# Optional: point to any OpenAI-compatible server
# OPENAI_BASE_URL=https://api.openai.com/v1
# Optional: system instruction
# OPENAI_SYSTEM_PROMPT=You are a poetic assistant in Russian
# Optional: set key directly instead of indirection
# OPENAI_API_KEY=your_api_key_here
# Optional: request timeout in milliseconds (default 30000)
# OPENAI_TIMEOUT_MS=30000
# Optional: retries for 429/5xx errors (default 2)
# OPENAI_MAX_RETRIES=2
# Optional: debug logs (1 to enable)
# OPENAI_DEBUG=1
